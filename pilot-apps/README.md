# VibeLang Pilot Applications

This directory contains Phase 14 pilot references used for GA-readiness
evidence:

- `service_reference/`: concurrent service-style batch processing app.
- `cli_tool_reference/`: CLI/tooling-oriented workload summary app.

Metrics and case studies are published under `reports/phase14/`.
