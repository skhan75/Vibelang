# Pilot App: CLI Tool Reference

This pilot models a CLI-oriented workload tracker:

- validates invariants with contracts,
- computes summary metrics over list inputs,
- prints deterministic outputs suitable for CI smoke checks.

Entry point: `pilot-apps/cli_tool_reference/main.yb`
