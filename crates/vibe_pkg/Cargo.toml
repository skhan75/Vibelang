[package]
name = "vibe_pkg"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
semver = "1"
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
tempfile = "3"
