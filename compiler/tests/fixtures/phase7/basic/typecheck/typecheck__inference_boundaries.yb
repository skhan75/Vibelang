pub type_inference_boundaries(x: Int) -> Int {
  @intent "exercise assignment inference boundaries and branches"
  y := x + 1
  y = y + 2
  if y > 3 {
    y = y + 1
  } else {
    y = y - 1
  }
  y
}
