# Chapter 9: Migration and Compatibility

Migration from `.vibe` to `.yb` is documented in:

- `docs/migrations/v1_0_source_extension_transition.md`
- `docs/policy/versioning_compatibility.md`

```vibe
pub main() -> Int {
  @effect io
  println("migrate with compatibility notes and CI evidence")
  0
}
```
