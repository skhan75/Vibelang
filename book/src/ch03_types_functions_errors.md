# Chapter 3: Types, Functions, and Errors

Functions can be typed explicitly and compose with deterministic diagnostics.

```vibe
pub add(a: Int, b: Int) -> Int {
  a + b
}
```

See `docs/spec/type_system.md` and `docs/spec/error_model.md` for normative
rules.
