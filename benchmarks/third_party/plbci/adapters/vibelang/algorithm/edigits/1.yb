pub main() -> Int {
  @effect io
  raw := fs.read_text(".benchmark_input")
  n := 27
  if raw != "" {
    parsed := json.parse_i64(raw)
    if parsed > 0 {
      n = parsed
    }
  }
  print(bench.edigits(n))
  return 0
}
