read_input() -> Str {
  @effect io
  raw := fs.read_text(".benchmark_input")
  if raw == "" {
    return "T_T"
  }
  return raw
}

pub main() -> Int {
  @effect io
  input := read_input()
  println("Hello world " + input + "!")
  0
}
