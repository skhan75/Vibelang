pub main() -> Int {
  @effect io
  raw := fs.read_text(".benchmark_input")
  n := 1
  if raw != "" {
    parsed := json.parse_i64(raw)
    if parsed > 0 {
      n = parsed
    }
  }
  println(bench.secp256k1(n))
  return 0
}
