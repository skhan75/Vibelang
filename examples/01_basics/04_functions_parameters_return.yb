add(a: Int, b: Int) -> Int {
  @examples {
    add(2, 3) => 5
    add(0, 0) => 0
    add(-1, 1) => 0
  }
  a + b
}

pub main() -> Int {
  @effect io
  total := add(2, 3)
  if total == 5 {
    println("fn-ok")
  } else {
    println("fn-bad")
  }
  0
}
