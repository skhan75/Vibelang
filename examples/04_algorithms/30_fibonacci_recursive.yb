fib(n: Int) -> Int {
  @examples {
    fib(0) => 0
    fib(1) => 1
    fib(5) => 5
  }
  if n <= 1 {
    return n
  }
  fib(n - 1) + fib(n - 2)
}

pub main() -> Int {
  @effect io
  if fib(8) == 21 {
    println("fib-rec-ok")
  } else {
    println("fib-rec-bad")
  }
  0
}
