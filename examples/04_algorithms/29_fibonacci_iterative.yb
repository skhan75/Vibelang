fib_iter(n: Int) -> Int {
  @examples {
    fib_iter(0) => 0
    fib_iter(1) => 1
    fib_iter(5) => 5
    fib_iter(10) => 55
  }
  if n <= 1 {
    return n
  }
  a := 0
  b := 1
  i := 2
  while i <= n {
    next := a + b
    a = b
    b = next
    i = i + 1
  }
  b
}

pub main() -> Int {
  @effect io
  if fib_iter(10) == 55 {
    println("fib-iter-ok")
  } else {
    println("fib-iter-bad")
  }
  0
}
