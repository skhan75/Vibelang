pub main() -> Int {
  @effect io
  digits := "1234"
  out := ""
  i := 0
  while i < digits.len() {
    out = out + "[" + digits[i:i + 1] + "]"
    i = i + 1
  }
  if out == "[1][2][3][4]" {
    println("str-build-ok")
  } else {
    println("str-build-bad")
  }
  0
}
