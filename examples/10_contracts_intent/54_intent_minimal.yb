score(x: Int) -> Int {
  @intent "increase score deterministically"
  x + 1
}

pub main() -> Int {
  @effect io
  if score(4) == 5 {
    println("intent-minimal-ok")
  } else {
    println("intent-minimal-bad")
  }
  0
}
